<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cập Nhật Người Dùng</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Tùy chỉnh Tailwind -->
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              primary: "#3B82F6",
              secondary: "#F59E0B",
              danger: "#EF4444",
              bgLight: "#F9FAFB",
            },
          },
        },
      };
    </script>
    <style>
      .form-container {
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }
      .form-control {
        transition: all 0.2s ease;
      }
      .form-control:focus {
        border-color: #3b82f6;
        box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
      }
    </style>
  </head>
  <body class="bg-bgLight min-h-screen py-6">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
      <h1 class="text-3xl font-bold text-gray-800 mb-6">Cập Nhật Người Dùng</h1>
      <a
        href="/get-crud"
        class="inline-block bg-gray-500 text-white font-semibold py-2 px-4 rounded-lg hover:bg-gray-600 transition mb-6"
        >Quay Lại Danh Sách</a
      >
      <div class="form-container bg-white rounded-lg p-6 max-w-lg mx-auto">
        <form action="/put-crud" method="POST" class="space-y-4">
          <input type="hidden" name="id" value="<%= user.id %>" />
          <div>
            <label for="email" class="block text-sm font-medium text-gray-700"
              >Email</label
            >
            <input
              type="text"
              name="email"
              id="email"
              value="<%= user.email %>"
              required
              class="form-control w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none"
            />
          </div>
          <div>
            <label
              for="firstName"
              class="block text-sm font-medium text-gray-700"
              >Họ</label
            >
            <input
              type="text"
              name="firstName"
              id="firstName"
              value="<%= user.firstName %>"
              required
              class="form-control w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none"
            />
          </div>
          <div>
            <label
              for="lastName"
              class="block text-sm font-medium text-gray-700"
              >Tên</label
            >
            <input
              type="text"
              name="lastName"
              id="lastName"
              value="<%= user.lastName %>"
              required
              class="form-control w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none"
            />
          </div>
          <div>
            <label for="address" class="block text-sm font-medium text-gray-700"
              >Địa chỉ</label
            >
            <input
              type="text"
              name="address"
              id="address"
              value="<%= user.address %>"
              required
              class="form-control w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none"
            />
          </div>
          <div class="flex space-x-4">
            <button
              type="submit"
              class="bg-primary text-white font-semibold py-2 px-4 rounded-lg hover:bg-blue-700 transition"
            >
              Cập Nhật
            </button>
            <a
              href="/get-crud"
              class="bg-gray-500 text-white font-semibold py-2 px-4 rounded-lg hover:bg-gray-600 transition"
              >Hủy</a
            >
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
