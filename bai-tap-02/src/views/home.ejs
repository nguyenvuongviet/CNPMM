<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Trang Chủ - Ứng Dụng CRUD</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Tùy chỉnh Tailwind -->
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              primary: "#3B82F6",
              secondary: "#F59E0B",
              danger: "#EF4444",
              bgLight: "#F9FAFB",
            },
          },
        },
      };
    </script>
    <style>
      .hero-container {
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }
      .btn {
        transition: all 0.2s ease;
      }
      .btn:hover {
        transform: translateY(-1px);
      }
    </style>
  </head>
  <body class="bg-bgLight min-h-screen py-6">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
      <div
        class="hero-container bg-white rounded-lg p-8 max-w-2xl mx-auto text-center"
      >
        <h1 class="text-4xl font-bold text-gray-800 mb-4">
          Chào Mừng Đến Với Ứng Dụng CRUD
        </h1>
        <p class="text-lg text-gray-600 mb-6">
          Quản lý thông tin người dùng một cách dễ dàng: tạo, xem, sửa, xóa dữ
          liệu.
        </p>
        <div class="flex justify-center space-x-4">
          <a
            href="/crud"
            class="btn bg-primary text-white font-semibold py-2 px-6 rounded-lg hover:bg-blue-700"
            >Tạo Người Dùng Mới</a
          >
          <a
            href="/get-crud"
            class="btn bg-secondary text-white font-semibold py-2 px-6 rounded-lg hover:bg-yellow-600"
            >Xem Danh Sách Người Dùng</a
          >
        </div>
      </div>
    </div>
  </body>
</html>
